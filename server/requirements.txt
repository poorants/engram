# Pinned exactly — a build that does not reproduce on the server is not a build.
fastapi==0.141.1
uvicorn[standard]==0.52.4
jinja2==3.1.6
psycopg[binary]==3.3.4
psycopg-pool==3.3.1
markdown-it-py==4.2.0
